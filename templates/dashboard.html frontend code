<!DOCTYPE html>
<html>
<head>
    <title>Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f4f6f9;
            margin: 0;
            padding: 30px;
        }

        h2 {
            margin-bottom: 20px;
        }

        .card {
            background: #fff;
            padding: 25px;
            border-radius: 8px;
            margin-bottom: 30px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }

        input, select {
            width: 100%;
            padding: 10px;
            margin: 8px 0;
            border-radius: 6px;
            border: 1px solid #ccc;
            font-size: 14px;
        }

        button {
            background: #5a73f2;
            color: white;
            padding: 10px 18px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 14px;
        }

        button:hover {
            background: #4059d6;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }

        th {
            background: #6a80f5;
            color: white;
            padding: 10px;
            text-align: center;
        }

        td {
            padding: 10px;
            text-align: center;
            border-bottom: 1px solid #ddd;
        }

        tr:hover {
            background: #f1f1f1;
        }
    </style>
</head>
<body>

{% if role == 'ADMIN' %}
<div class="card">
    <h2>Add Employee</h2>
    <form method="post" action="/add_employee?user={{ user }}&role={{ role }}">
        <input name="name" placeholder="Employee Name" required>
        <input name="department" placeholder="Department" required>
        <input name="email" placeholder="Email" required>
        <input name="salary" placeholder="Salary" required>

        <select name="status">
            <option>Active</option>
            <option>On Leave</option>
            <option>Resigned</option>
        </select>

        <button type="submit">Add Employee</button>
    </form>
</div>
{% endif %}

<div class="card">
    <h2>Employee List</h2>
    <table>
        <tr>
            <th>Emp Code</th>
            <th>Name</th>
            <th>Department</th>
            <th>Email</th>
            <th>Salary</th>
            <th>Status</th>
        </tr>

        {% for e in employees %}
        <tr>
            <td>{{ e[1] }}</td>
            <td>{{ e[2] }}</td>
            <td>{{ e[3] }}</td>
            <td>{{ e[4] }}</td>
            <td>{{ e[5] }}</td>
            <td>{{ e[6] }}</td>
        </tr>
        {% endfor %}
    </table>
</div>

</body>
</html
